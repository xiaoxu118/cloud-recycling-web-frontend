# 角色与成员管理设计方案

> **实现状态（2026-09 更新）**：本方案已落地。后端在 `cloud-recycling/cloudfunctions/quickstartFunctions/recycle/members.js`（成员接口 + 双写 + 迁移）与 `roles.js`（权限目录、内置角色 `admin` / `store_manager` / `recycler` / `support`），前端在 `src/figma/FigmaAdmin.tsx` 的 `MembersPage` / `RolesPage` / `StaffRecruitsPage` 与 `AuthContext` 权限模型。初始化与迁移走 `initMemberCollections` / `migrateMembersFromLegacy`。与本文档的两处偏差：① 成员状态实际为 `active` / `resting` / `resigned`（本文一致）；② 回收员身份判定已切换为 members 优先、旧 `staff` 回退（见 `staff.js` 的 `resolveMemberAsStaff`）。以下为原始设计正文。

## 一、导航结构

```
侧边栏
├── 订单管理        /orders
├── 人员管理 ▾
│   ├── 用户管理    /users        C 端微信用户
│   ├── 成员管理    /members      后台用户 + 工作人员
│   └── 角色管理    /roles        角色与权限配置
├── 品类管理        /categories
├── 分析统计        /analytics
├── 投诉建议        /feedback
├── 积分管理        /points
└── 系统配置        /settings
```

路由映射：

| 路径 | 页面 |
|------|------|
| `/users` | 用户列表 |
| `/users/:id` | 用户详情（不显示任何成员/角色信息） |
| `/members` | 成员列表 |
| `/members/:id` | 成员详情（可选展示关联的 C 端账号） |
| `/roles` | 角色列表 |

`/staff` 保留重定向到 `/members`。

## 二、数据模型

### 2.1 `members` 集合（后台人员统一档案）

**`_id` 直接复用 `staff._id`**，确保 `orders.recyclerId` 零改动。

| 情况 | `members._id` 取值 |
|------|-------------------|
| 手机号在 `staff` 中存在 | `staff._id`（优先） |
| 手机号仅在 `admins` 中存在 | `admins._id` |
| 两边都有（同一手机号） | `staff._id` |
| 新建成员 | 数据库自动生成 |

**字段列表**：

```json
{
  "_id": "沿用 staff._id / admins._id / 新建时自动生成",
  "name": "张三",
  "phone": "13800138000",
  "roleKeys": ["recycler"],
  "status": "active",
  "employeeNo": "S20240001",
  "store": "宝山店",
  "area": "宝山区",
  "joinDate": "2024-01-01",
  "openid": "o_xxx",
  "cloudbaseUid": "xxx",
  "wechatBound": false,
  "createTime": 1700000000000,
  "updateTime": 1700000000000
}
```

**字段说明**：

| 字段 | 必填 | 索引 | 说明 |
|------|------|------|------|
| `_id` | 是 | 主键 | 复用 `staff._id` 或 `admins._id`，新建自动生成 |
| `name` | 是 | — | 姓名 |
| `phone` | 是 | 唯一 | 手机号，关联 `users` 用 |
| `roleKeys` | 是 | — | 角色数组，如 `["admin", "recycler"]` |
| `status` | 是 | — | `active` 在职 / `resting` 休息 / `resigned` 离职 |
| `employeeNo` | 否 | — | 工号 |
| `store` | 否 | — | 所属门店 |
| `area` | 否 | — | 服务区域 |
| `joinDate` | 否 | — | 入职日期 |
| `openid` | 否 | — | 微信 OpenID，小程序端首次识别后回填 |
| `cloudbaseUid` | 否 | — | CloudBase Auth UID，Web 端手机号登录后回填 |
| `wechatBound` | 否 | — | 是否已绑定微信 |
| `createTime` | 是 | — | 创建时间戳 |
| `updateTime` | 是 | — | 更新时间戳 |

**`status` 与旧 `staff.status` 对应关系**：

| staff.status | members.status |
|-------------|----------------|
| `online` | `active` |
| `resting` | `resting` |
| `resigned` | `resigned` |

### 2.2 `roles` 集合（角色与权限定义）

```json
{
  "_id": "自动生成",
  "roleKey": "store_manager",
  "name": "店长",
  "description": "管理门店订单与回收员",
  "permissions": ["order:read", "order:assign", "member:read"],
  "builtIn": true,
  "sort": 30,
  "createTime": 1700000000000,
  "updateTime": 1700000000000
}
```

**字段说明**：

| 字段 | 必填 | 索引 | 说明 |
|------|------|------|------|
| `_id` | 是 | 主键 | 自动生成 |
| `roleKey` | 是 | 唯一 | 角色标识，不可修改 |
| `name` | 是 | — | 角色显示名称 |
| `description` | 否 | — | 角色说明 |
| `permissions` | 是 | — | 权限点数组，可配置 |
| `builtIn` | 是 | — | 内置角色不可删除，权限可调 |
| `sort` | 否 | — | 排序号 |
| `createTime` | 是 | — | 创建时间戳 |
| `updateTime` | 是 | — | 更新时间戳 |

### 2.3 内置角色预设

| roleKey | 名称 | 说明 | 权限点 |
|---------|------|------|--------|
| `super_admin` | 超级管理员 | 全部权限，不可删除 | 全部 |
| `admin` | 管理员 | 运营全权限，不含角色管理 | 除 `role:write` 外全部 |
| `store_manager` | 店长 | 门店级别运营 | 订单读写派单、成员只读、品类只读、用户只读、统计 |
| `recycler` | 回收员 | 仅小程序接单 | 无后台权限 |
| `support` | 客服 | 客服角色 | 订单只读、投诉读写、用户只读、积分只读 |

### 2.4 权限点清单

```
订单模块：  order:read   order:write   order:assign   order:delete
成员模块：  member:read  member:write
角色模块：  role:read    role:write
用户模块：  user:read    user:detail
品类模块：  category:read  category:write
配置模块：  setting:read   setting:write
投诉模块：  feedback:read  feedback:write
积分模块：  points:read    points:adjust
统计模块：  analytics:read
```

### 2.5 与 `users` 的关系

```
users（不动）                       members（新）
 ├ openid      主键                  ├ _id         主键（复用 staff._id）
 ├ nickName                          ├ name
 ├ avatarUrl                         ├ roleKeys
 ├ phone  ──────── phone 关联 ─────→  ├ phone
 ├ points                            ├ store
 └ ...                               └ ...
「这个微信是谁」                      「这个人能干什么」
```

- **用户详情页**：不显示任何成员/角色信息
- **成员详情页**：按 `phone` 查 `users`，命中时显示昵称/头像/积分/下单数，用于确认微信绑定情况；未命中显示「未绑定微信」

## 三、鉴权改造

### 3.1 `assertAdminSession` 扩展

查找顺序：`members`（按 `cloudbaseUid` / `openid`）→ 未命中回退 `admins`（视为 `super_admin`）。

返回值：

```js
{
  ok: true,
  admin: { name, role },           // 保留，兼容现有 17 处调用点
  member: { _id, name, phone, roleKeys, status, store },
  permissions: Set<string>,        // roleKeys 对应权限的并集
  session
}
```

### 3.2 权限断言辅助函数

```js
const assertPermission = (auth, perm) =>
  auth.permissions.has(perm) ? { ok: true } : { ok: false, errMsg: "NO_PERMISSION" };
```

接口按需调用，前端也按权限点控制按钮显隐。

### 3.3 `resolveStaff` 改造

小程序端 `resolveStaff` 加前置分支：

1. 先查 `members`（`roleKeys` 含 `recycler` 且 `status === "active"`）
2. 命中 → 返回 `{ _id: member._id, ... }`（`_id` 值与 `staff._id` 一致，抢单/派单逻辑不用改）
3. 未命中 → 回退旧 `staff` 集合

## 四、迁移策略

### 4.1 迁移脚本（幂等）

```
遍历 staff 集合：
  members.doc(staff._id) 已存在 → 合并 roleKeys 加 "recycler"，更新快照字段
  不存在 → add({ _id: staff._id, roleKeys: ["recycler"], status: 映射(staff.status), ... })

遍历 admins 集合：
  按 phone 查 members
    命中 → roleKeys 追加 "admin"，补 cloudbaseUid / openid / wechatBound
    未命中 → add({ _id: admins._id, roleKeys: ["admin"], status: admins.enabled ? "active" : "resigned", ... })

收尾：
  若不存在任何 roleKeys 含 "super_admin" 的成员，把最早创建的 admin 升为 super_admin
```

可反复执行，结果一致，旧集合只读不改。

### 4.2 双写规则（过渡期）

`adminSaveMember` 写 `members` 后同步旧集合：

```
roleKeys 含 recycler
  → staff.doc(member._id).update() 或 add({ _id: member._id, ... })
  → 姓名/手机号变更时调 syncOrdersRecyclerInfo 回刷订单快照

roleKeys 含 admin / super_admin
  → admins 按 phone 查重，命中则 update，未命中则 add

移除 recycler 角色 → staff.status 置 resigned（不删记录）
移除 admin 角色   → admins.enabled 置 false（不删记录）
```

### 4.3 历史数据清理

当未来遇到历史数据一致性问题时，直接：

1. 清查 `members` 与 `staff`/`admins` 的差异
2. 重新跑迁移脚本覆盖写入
3. 确认无误后删除 `staff` 和 `admins` 集合（或保留为只读备份）

## 五、安全约束

1. **最后超管保护**：不允许停用或移除最后一个 `super_admin`
2. **不可自我降权**：成员不能编辑自己的 `roleKeys` 和 `status`
3. **角色提权限制**：只有 `super_admin` 能授予 `super_admin` 角色
4. **手机号唯一**：`members.phone` 唯一索引，新增时查重
5. **`roleKey` 不可修改**：编辑接口忽略传入的 `roleKey`
6. **内置角色不可删除**：`builtIn: true` 的角色删除按钮隐藏
7. **`super_admin` 权限锁死**：其 `permissions` 不可编辑，确保不会被误操作删掉自身权限

## 六、云函数接口清单

### 新增接口

| 接口 | 权限点 | 说明 |
|------|--------|------|
| `initMemberCollections` | — | 建集合 + 写入内置角色种子数据，一次性 |
| `migrateMembersFromLegacy` | `super_admin` | 幂等迁移脚本 |
| `adminListMembers` | `member:read` | 分页 + 按角色/状态/门店/关键词筛选 |
| `adminGetMemberDetail` | `member:read` | 含关联的 C 端账号信息 |
| `adminSaveMember` | `member:write` | 新增/编辑，双写旧集合 |
| `adminToggleMemberStatus` | `member:write` | 启停，含最后超管保护 |
| `adminListRoles` | `role:read` | 角色列表 + 每个角色的成员数 |
| `adminSaveRole` | `role:write` | 新增/编辑角色与权限 |
| `adminDeleteRole` | `role:write` | 内置角色禁删；有成员在用则禁删 |
| `adminGetPermissionCatalog` | `role:read` | 权限点分组清单，供前端渲染 |
| `adminGetAuthInfo` | — | 返回当前登录者的 member + permissions，供前端初始化 |

### 保留不动

`adminListStaff`、`adminSaveStaff`、`adminListAdmins`、`adminSaveAdmin`、`adminToggleAdmin` 全部保留，作为回滚兜底。

## 七、前端页面设计

### 7.1 用户管理 `/users`

沿用现有用户列表，不加任何成员相关的字段或入口。

表格列：用户（头像 + 昵称）/ 联系电话 / 微信状态 / 订单数 / 地址数 / 积分 / 创建时间 / 最近活跃

### 7.2 成员管理 `/members`

顶部统计卡：在职 / 休息 / 离职

筛选栏：关键词搜索（姓名/手机号/工号）+ 角色下拉 + 状态下拉 + 门店下拉

MUI Table 列：

| 列 | 内容 |
|----|------|
| 成员 | 姓名 + 工号（小字灰色） |
| 手机号 | 等宽字体 |
| 角色 | 多个 `Chip`，超管红色、管理员蓝色、店长橙色、回收员绿色、客服灰色 |
| 门店 | 文本 |
| 状态 | `Chip`（在职/休息/离职） |
| 微信 | 已绑定 / 未绑定 |
| 入职日期 | 文本 |

整行点击进详情，右上角「添加成员」按钮。

编辑弹窗（MUI `Dialog`）：姓名 / 手机号 / 工号 / 角色（多选 `Checkbox` 组）/ 状态 / 门店 / 服务区域 / 入职日期

### 7.3 角色管理 `/roles`

卡片列表或表格，每行：角色名 / roleKey / 权限数 / 成员数 / 操作

编辑弹窗：
- 角色名 / 说明（可编辑）
- `roleKey`：内置角色禁用，自定义角色可编辑
- 权限勾选矩阵：按模块分组的 `FormGroup` + 全选，`super_admin` 的权限勾选禁用
- 删除按钮：内置角色隐藏，有成员在用的自定义角色禁用并提示「该角色下有 N 名成员，请先移除」

### 7.4 权限驱动的 UI

前端登录后从 `adminGetAuthInfo` 拿到 `member` + `permissions`，存入 Context。

侧边栏菜单项和页面内的操作按钮按权限点控制显隐，无权限的页面直接重定向到有权限的首个页面。

## 八、实施顺序

```
1. 云函数：新建 members.js + roles.js，实现 initMemberCollections 与内置角色种子数据
2. 云函数：迁移脚本 migrateMembersFromLegacy（幂等，可反复跑）
3. 云函数：members / roles 的 CRUD 接口（含双写旧集合）
4. 云函数：assertAdminSession 扩展 permissions，resolveStaff 加 members 前置分支
5. 前端：侧边栏改为支持二级菜单，拆分三个页面路由
6. 前端：成员管理页 + 角色管理页（MUI）
7. 前端：权限 Context、adminGetAuthInfo 对接、按钮级显隐
8. 灰度：跑迁移脚本 → 双集合数据核验 → 确认一致后启用新页面
```

前 4 步纯新增，线上零风险；第 5 步之后有双写兜底，可随时回退。